const createStore = (reducer) => {
  let state;

  function dispatch() {
    // coding
  }

  function getState() {
    // coding
  }

  return {
    dispatch,
    getState,
  };
};

let store = createStore(() => {});

console.log(store);
